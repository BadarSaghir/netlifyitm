import { LOAD_CONTENT } from "../constants/ActionTypes";
import data from "../data.json";

const initialState = data;

const contentReducer = (state = initialState, action) => {
  return state;
};

export default contentReducer;
